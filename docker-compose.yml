name: neochef
services:
  server:
    image: ghcr.io/pavle2002/neochef:latest
    env_file:
      - .env
    depends_on:
      - redis
    restart: unless-stopped

  import-cron:
    image: ghcr.io/pavle2002/neochef:latest
    command: ["sh", "server/import-cron.sh"]
    env_file:
      - .env
    volumes:
      - import_progress:/app/data
    restart: unless-stopped
    depends_on:
      - redis

  redis:
    image: redis:alpine
    restart: unless-stopped
    volumes:
      - redis_data:/data

  caddy:
    image: caddy:alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - caddy_data:/data
      - caddy_config:/config
    restart: unless-stopped
    depends_on:
      - server

volumes:
  redis_data:
  caddy_data:
  caddy_config:
  import_progress:
