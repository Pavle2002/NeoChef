{
  "name": "neochef",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "common"
  ],
  "scripts": {
    "build:all": "npm run build -w common && npm run build -w server && npm run build -w client",
    "dev:client": "npm run dev -w client",
    "dev:server": "npm run dev -w server"
  }
}
